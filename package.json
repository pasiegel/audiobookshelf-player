{
  "name": "audiobookshelf-player",
  "version": "1.0.0",
  "description": "Electron wrapper for Audiobookshelf",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . audiobookshelf-player --platform=win32 --arch=x64 --out=dist --overwrite --icon=icon.ico"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-packager": "^17.1.1"
  }
}
